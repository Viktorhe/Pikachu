import React from 'react';
import Loading from '../../../tenor.gif'

const Loads = () =>{
    return (
        <div className="load">
            <img src={Loading} alt="prop"></img>
        </div>
    )
}

export default Loads;